# Copy to .env and fill in. Never commit .env.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Local HTTP bridge used by Cursor hooks (loopback only).
BRIDGE_HOST=127.0.0.1
BRIDGE_PORT=8765
BRIDGE_API_TOKEN=

# How long to wait for a Telegram reply (seconds).
ASK_TIMEOUT_SEC=900
